/**
 * The template this app opens on.
 *
 * `framework` = the shipped demo registry (what a fresh clone shows).
 * A scaffolded app rewrites this one line to its chosen template id, so a
 * generated project boots straight into its own app with no user action.
 *
 * Rewritten by: node scripts/shell-cli.js create <dir> --template <id>
 */
export const DEFAULT_TEMPLATE_ID = 'framework'
